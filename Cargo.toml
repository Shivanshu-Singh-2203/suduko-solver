[package]
name = "suduko-solver"
version = "0.1.0"
edition = "2024"

[dependencies]
# The SAT Solver core
varisat = "0.2.2"

# Command Line Argument Parsing (Modern Derive API)
clap = { version = "4.5", features = ["derive"] }
indicatif = "0.17"